# BetterResumeReviewer

## setup frontent ##
Run locally: cd frontend -> npm install

```bash
npm  run  dev
```
## setup backend ##
Run locally: cd backend -> python -m venv venv -> source venv/bin/activate -> pip install -r requirements.txt

```bash
uvicorn main:app --reload


